import React from 'react';

const Home: React.FC = () => {
  return (
    <div>
      <h1>Chào mừng đến với NovaTech</h1>
      <p style={{ marginTop: '1rem', color: 'var(--text-muted)' }}>
        Hệ thống đã được thiết kế lại hoàn toàn trên nền tảng React + Supabase.
      </p>
    </div>
  );
};

export default Home;
